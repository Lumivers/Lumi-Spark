# Lumi Spark — 代码生成规则

本项目是一个 Unreal Engine 游戏项目。项目所有者正处于进阶学习阶段，每次生成或修改代码时，必须在代码之外附带简明扼要的**设计讲解**，帮助其理解"为什么这么写"以及游戏开发中的关键注意事项。

---

## 代码讲解要求

每次生成或修改代码时，在代码输出**之后**附带一个 `## 💡 设计笔记` 小节，包含以下内容（按需选择，不必每次全部覆盖）：

### 1. 为什么要写这段代码
- 这段代码解决了什么问题，在游戏架构中扮演什么角色。

### 2. 为什么这么设计
- 为什么选择了当前的类层级 / 模式 / 组件划分。
- 有没有其他方案，为什么没选。

### 3. UE 框架契合度
- 这段代码如何融入 UE 的框架设计哲学（Actor-Component 模型、GameMode/GameState/PlayerState 职责分离、Subsystem 的使用等）。
- 是否有 UE 已提供的基础设施可以复用，避免重复造轮子。

### 4. 游戏开发特有的注意事项（按相关性提及）
- **性能**：是否有 Tick 开销、是否应改用 Timer/事件驱动、是否需要对象池。
- **网络**：哪些逻辑跑在 Server / Client，复制属性的带宽考量，Authority 判断。
- **生命周期**：初始化顺序（Constructor → PostInitializeComponents → BeginPlay），CDO 陷阱，组件依赖顺序。
- **内存**：强引用 vs 软引用、资源加载策略、GC 影响。
- **代码与蓝图边界**：哪些部分适合暴露给蓝图，UPROPERTY/UFUNCTION 的 Specifier 选择理由。

### 5. 常见陷阱提醒
- 针对当前代码场景，主动提醒可能踩到的坑（空指针、初始化顺序、网络不同步等）。

---

## 代码风格要求

- 遵循 UE 编码规范（类名 U/A/F/S/E 前缀，UPROPERTY/UFUNCTION 宏等）。
- 使用组件化设计优先于深继承。
- 数值型配置使用 DataTable / DataAsset / EditAnywhere，不硬编码。
- 使用 GameplayTag 做状态标记，避免大量 bool。
- 模块间通信优先使用 Delegate/Event，避免直接强引用。
- 代码中的注释使用中文（与项目所有者母语一致）。

---

## 讲解风格

- 语言：中文。
- 语气：自然、像同事间交流，不需要太正式。
- 详细程度：中等偏详细。不需要把每个基础概念都解释一遍，但关键设计决策和"为什么"要讲清楚。
- 如果涉及的知识点比较进阶（如 GAS、网络同步细节、内存管理），可以适当多讲一些。
